# `ft_irc` Historical Reference Case

**Case ID:** `ft-irc-reference-001`  
**Framework:** foundation `v0.1`  
**Protocol:** `context-init v0.2`  
**Recorded mode:** Began as `Planning`; changed to `Full-cycle` without a
formalized transition  
**Status:** `CLOSED`  
**Score:** `91.3/100`  
**Verdict:** `PASS`  
**Reference-grade:** No  
**Rerun authorized:** No

## Purpose

This case tested the first ShapingTheAxe foundation protocol on a real software
project with a formal subject, existing code and history, strict constraints,
personal style requirements, and collaborator ownership boundaries.

It is historical evidence for the framework. It is not the central subject of
the beta and does not demonstrate multidomain universality.

## Frozen result

The approved independent evaluation records:

- score `91.3/100`;
- verdict `PASS`;
- no hard failure;
- independent handoff passed;
- exact correspondence between framework, subject, repository snapshot,
  technical report, and evidence;
- `irssi` blocked by the environment and documented;
- defects attributed to the experimental fixture, not to unrelated real
  projects.

The case is not Reference-grade because:

- the complete chronological transcript was not retained;
- the `Planning → Full-cycle` change was not formally recorded;
- the real `irssi` check remains pending due to the documented environment
  limitation.

## Frozen input identity

| Input | Frozen identity | Size | Role |
|---|---|---:|---|
| `local_IRC_auditoria.tar.gz` | SHA-256 `83b132b9b5ad0cc8532f50e9338bbc99129204b32ddf6c59baee1e3c6f94fced` | 105,598 bytes | Repository snapshot including git history |
| `Irc.pdf` | SHA-256 `216877566d020d68177ca9a447e0f725b0edc1a719218ea3322f663dcdbf9c9b` | 1,362,415 bytes | Official subject, version 10.0, 13 pages |
| Repository `HEAD` | Git commit `7fa8e6831b5b4274d259a97d10264d92a46cdaf1` | 34 tracked files | Exact evaluated code and history |

The official PDF was authoritative over convenience conversions.

## Human contract used by the case

- Audit the frozen project against the official subject.
- Distinguish functional defects, subject violations, style observations, and
  non-blocking improvements.
- Inspect tracked files for local AI-workflow artifacts without inventing an
  AI-use conclusion.
- Respect the collaborator boundary: Zabal owned the network and client
  lifecycle area; Jessica owned parsing, IRC commands, channels, users, and
  operator permissions.
- Preserve integration interfaces and avoid rewriting collaborator-owned code
  merely for stylistic uniformity.
- Use C++98 and the official subject constraints.

## Evidence boundary

This beta package does not contain the private repository, official subject,
complete transcript, technical execution report, or evidence bundle. Their
absence is not repaired with placeholders.

Do not:

- repeat the audit;
- modify the frozen fixture;
- reconstruct missing conversation evidence;
- create empty `context-brief.md`, `implementation-plan.md`, or `evaluation.md`
  files;
- promote the result to Reference-grade;
- attribute fixture defects to an unrelated project.

The original foundation `v0.1` archive preserves the pre-run case procedure
and pending status as historical source text. This file records the later
approved closure state without rewriting that archive.

## Relationship to beta validation

The case supports these conclusions:

- the predecessor protocol can produce a `PASS` execution and successful
  handoff;
- complete chronological state transitions matter;
- environment limitations must remain distinct from protocol and fixture
  defects.

It does not support these conclusions:

- universal multidomain effectiveness;
- superiority over brainstorming or normal flow;
- validated proportional budget selection;
- automated learning or capability governance.

Use new frozen cases for the comparative beta protocol. Do not rerun this one.
